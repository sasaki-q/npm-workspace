{
  "name": "workspace",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:server": "npm run dev -w @workspace/api",
    "build:server": "npm run build -w @workspace/api",
    "compile:domains": "npm run compile -w @workspace/domains",
    "compile:infra": "npm run compile -w @workspace/infrastructure",
    "run:migration": "npm run run:migration -w @workspace/batch"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [ "packages/*" ],
  "dependencies": {
    "ts-node": "^10.9.1"
  }
}
